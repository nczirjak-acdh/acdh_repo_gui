(function ($, Drupal) {
    'use strict';

    Drupal.behaviors.acdhRepoGui = {
        attach: function (context, settings) {
            console.log("hello acdh gui");
        }
    };
})(jQuery, Drupal);
 
